/dist/
/obj/
*.o
/scratch/

# addon/ ships prebuilt binaries deliberately (forceAudioIn.so, injectTone)
# so the addon folder is ready to deploy as-is - don't ignore those.

# Local-only files (not for the public repo)
*Zone.Identifier
/docs/Force_MockbaMod_WiFi_Handoff.md*
